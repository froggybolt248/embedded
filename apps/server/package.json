{
  "name": "@embedded/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch --import tsx src/main.ts",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@embedded/calc": "workspace:*",
    "@embedded/core": "workspace:*",
    "@embedded/db": "workspace:*",
    "@embedded/ingest": "workspace:*",
    "@embedded/llm": "workspace:*",
    "@embedded/rules": "workspace:*",
    "@embedded/tools": "workspace:*",
    "@fastify/multipart": "^10.1.0",
    "@fastify/static": "^8.1.0",
    "fastify": "^5.3.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
